# custom-blocks.conf — Blocages manuels (séparé de la liste Peter Lowe)
# Ne pas modifier unbound_add_servers.conf directement — ce fichier survit aux mises à jour

    # Unity Ads — peter lowe n'a que configv2, on bloque le domaine parent
    # Couvre : webview.unityads.unity3d.com, configv2.unityads.unity3d.com, etc.
    local-zone: "unityads.unity3d.com" redirect
    local-data: "unityads.unity3d.com A 127.0.0.1"

    # Cookiebot (Cybot/Usercentrics) — bannières GDPR + trackers de consentement
    # Couvre : consentcdn.cookiebot.com, consent.cookiebot.com, etc.
    local-zone: "cookiebot.com" redirect
    local-data: "cookiebot.com A 127.0.0.1"
